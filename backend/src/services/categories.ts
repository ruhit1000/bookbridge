import { Router } from "express";

const router = Router();

// ─── Phase 9 — Category Routes ─────────────────────────────────
// POST   /api/v1/categories
// GET    /api/v1/categories
// GET    /api/v1/categories/:id
// PATCH  /api/v1/categories/:id
// DELETE /api/v1/categories/:id

export default router;
