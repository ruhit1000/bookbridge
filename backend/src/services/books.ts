import { Router } from "express";

const router = Router();

// ─── Phase 10 — Book Routes ────────────────────────────────────
// POST   /api/v1/books
// GET    /api/v1/books
// GET    /api/v1/books/:id
// PATCH  /api/v1/books/:id
// DELETE /api/v1/books/:id

export default router;
